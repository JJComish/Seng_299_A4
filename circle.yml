machine:

  timezone:
    America/Los_Angeles #Set the timezone

  # Version of python to use
  python:
    version: 3.6.0


test:
  override:
    - py.test

dependencies:
  pre:
    - pip install pytest
 